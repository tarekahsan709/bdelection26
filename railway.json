{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd app && npm install && npm run build && cp -r public .next/standalone/public && cp -r .next/static .next/standalone/.next/static"
  },
  "deploy": {
    "startCommand": "HOSTNAME=0.0.0.0 node app/.next/standalone/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
